from datasets import load_dataset

ds = load_dataset("toxigen/toxigen-data", "annotated")

Copy
from datasets import load_dataset

ds = load_dataset("toxigen/toxigen-data", "annotations")

Copy
from datasets import load_dataset

ds = load_dataset("toxigen/toxigen-data", "prompts")
